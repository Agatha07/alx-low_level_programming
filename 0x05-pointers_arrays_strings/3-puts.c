#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string followed by a new line to stdout
 * @str: String to print
 */

void _puts(char *str)
{
	int n = 0;

	while (s[n] != '\0')
	{
		/*_putchar(str[n])*/
		n++;
	}
	_putchar('\n');
}
